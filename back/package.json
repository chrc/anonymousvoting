{
  "name": "anonymousvoting2",
  "version": "1.0.0",
  "description": "",
  "directories": {
    "test": "test"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "ganache": "./node_modules/.bin/ganache-cli --gasLimit=0x1fffffffffffff --allowUnlimitedContractSize -e 1000000000",
    "compile": "./node_modules/.bin/truffle compile --all",
    "migrate": "./node_modules/.bin/truffle migrate --reset"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "ganache-cli": "^6.7.0",
    "truffle": "^5.0.41"
  }
}
